<thlogic>
    <attr sel="#headId"
         th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#welcome" th:text="#{home.welcome}"/>
    <attr sel="#playId"
          th:href="@{/play}"
          th:text="#{home.play}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
</thlogic>